export default class SidebarNavLink {
  /**
   *
   * @param {String} text
   * @param {String} to
   * @param {String} icon
   */
  constructor(text, to, icon) {
    this.text = text;
    this.to = to;
    this.icon = icon;
  }
}
